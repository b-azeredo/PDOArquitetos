import type { RequiredDataFromCollectionSlug } from 'payload'

// Empty array for now - projects will be created through the admin panel
// since they require media uploads (featuredImage is required)
export const projectsStatic: RequiredDataFromCollectionSlug<'projects'>[] = []